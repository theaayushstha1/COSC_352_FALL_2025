version: '3.8'

services:
  turing-machine:
    build: .
    container_name: turing_machine_simulator
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
