# Use official Python image
FROM python:3.11-slim

# Set working directory inside container
WORKDIR /app

# Copy script into container
COPY read_html_table.py .

# Default command to run program
ENTRYPOINT ["python", "read_html_table.py"]
